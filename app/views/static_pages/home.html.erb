
<% if signed_in? %>

    <%= render 'users/user_home' %>
    <section>
        <% render 'shared/user_info' %>
    </section>
    <section>
        <% render 'shared/stats' %>
    </section>
    <section>
        <% render 'shared/micropost_form' %>
    </section>

<% else %>

    <%= render 'main_home' %>

<% end %>

